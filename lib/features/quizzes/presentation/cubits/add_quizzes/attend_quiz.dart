import 'package:flutter/material.dart';

class AttendQuiz extends StatelessWidget {
  const AttendQuiz({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
